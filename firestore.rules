rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default: Authenticated users have read/write access to app collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
